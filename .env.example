############################################
# YNAB Rewards Tracker - Environment Variables
# 
# This app stores all user data in browser localStorage.
# The only env variable needed is for optional database caching.
############################################

# Optional: Database for caching (not required for basic usage)
# SQLite (default for development):
DATABASE_URL=file:../packages/db/dev.db

# Or PostgreSQL for production:
# DATABASE_URL=postgresql://user:pass@localhost:5432/ynab_rewards

############################################
# Note: YNAB Personal Access Token is now stored 
# in browser localStorage via the Settings page.
# No server-side token storage or encryption needed.
############################################